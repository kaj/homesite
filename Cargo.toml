[package]
name = "homesite"
version = "0.1.0"
authors = ["Rasmus Kaj <kaj@kth.se>"]

build = "src/build.rs"

[build-dependencies]
ructe = { version = "^0.3.6", features = ["sass", "mime02"] }

[dependencies]
iron = "0.6.0"
router = "0.6.0"
mime = "0.2.6"
