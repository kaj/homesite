[package]
name = "homesite"
version = "0.1.0"
authors = ["Rasmus Kaj <kaj@kth.se>"]

build = "src/build.rs"

[build-dependencies]
ructe = { version = "^0.3.6", features = ["sass", "mime03"] }

[dependencies]
gotham = "~0.1.0"
gotham_derive = "~0.1.0"
hyper = "*"
mime = "~0.3.0"
log = "*"
